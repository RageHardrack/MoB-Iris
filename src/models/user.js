export default class User {
	constructor(nombre, username, email, empresa, password, roles) {
		this.nombre = nombre;
		this.username = username;
		this.email = email;
		this.empresa = empresa;
		this.password = password;
		this.roles = roles;
	}
}
